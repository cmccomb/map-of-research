# Validated snapshots

The automation branch stores the lossless profile-publication Parquet snapshot
and checksum manifest here. The manifest binds all three registry files as one
digest. Raw snapshots never contain Hugging Face credentials or model code.

# Security policy

Please report vulnerabilities privately through GitHub's security advisory
feature.

This site has no credentials or server-side code. It allows browser connections
only to the versioned public dataset on `huggingface.co` and loads no third-party
scripts.
